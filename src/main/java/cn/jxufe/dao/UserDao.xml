<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.jxufe.dao.UserDao">
    <!--注意resultType注重的不是成员组成的
    对象，所以这里填list会报Cause: java.lang.UnsupportedOperationException错-->

    <select id="getPasswordByEmail" parameterType="string" resultType="string">
        select password_email
        from user
        where email = #{email}
        limit 1;
    </select>

    <select id="getPasswordByTel" parameterType="string"  resultType="string">
        select password_tel
        from user
        where tel = #{tel}
        limit 1;
    </select>

    <!--选择user表中与用户类属性的对应的字段-->
    <sql id="selectUserInfo">
        select user_no, username, email, tel, avatar, self_summary,
               gender,follow_num, follower_num, blog_num, word_num, like_num
        from user
    </sql>

    <!--三种方式获取用户信息-->
    <select id="getUserByTel" parameterType="string" resultType="user">
        <include refid="selectUserInfo"/>
        where tel = #{tel};
    </select>
    <select id="getUserByEmail" parameterType="string" resultType="user">
        <include refid="selectUserInfo"/>
        where email = #{eamil};
    </select>
    <select id="getUserByUserNo" parameterType="int" resultType="user">
        <include refid="selectUserInfo"/>
        where user_no = #{user_no};
    </select>

    <!--注册用户添加信息-->
    <insert id="insertUser" parameterType="user" useGeneratedKeys="true" keyProperty="userNo">
        insert into user (username, email, tel, password_tel, password_email, avatar)
        values (#{username}, #{email}, #{tel}, #{passwordTel}, #{passwordEmail}, #{avatar});
    </insert>
</mapper>